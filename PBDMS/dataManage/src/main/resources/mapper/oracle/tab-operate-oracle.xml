<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzsj.dm.manage.mapper.oracle.TabOperateOacleMapper">

    <!--根据表名删除表信息-->
    <select id="deleteTab" parameterType="java.lang.String">
        ${tableEnName}
    </select>

    <!--插入数据-->
    <insert id="insertData" parameterType="java.lang.String">
        ${sql}
    </insert>

    <update id="createTab" parameterType="java.lang.String">
        ${sql}
    </update>
</mapper>